<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itsme.com.moviecatalogue">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:parentActivityName=".FirstActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="itsme.com.moviecatalogue.FirstActivity" />
        </activity>

        <activity
            android:name=".SharedPrefrenceActivity"
            android:label="@string/sharedPrefTitle"
            android:parentActivityName=".FirstActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".FirstActivity" />
        </activity>

        <service android:name=".Service.FetchDataService" />
        <service android:name=".Service.GetMovieDetailsService" />

        <provider
            android:name=".Data.MovieProvider"
            android:authorities="itsme.com.moviecatalogue"
            android:exported="false" />

    </application>

</manifest>
